import React from 'react';
import PropTypes from 'prop-types';
import './Book.css';

const Book = () => (
  <div id="book" className="Book">
    Book Component
  </div>
);

Book.propTypes = {};

Book.defaultProps = {};

export default Book;
